var todos = ['item 1', 'item 2', 'item 3'];
    console.log('myTodos:', todos);
    myTodos: ['item 1', 'item 2', 'item 3'];
todos.push('item 4');
    console.log('myTodos:', todos);
todos;
    

// var hotels = {
   // rooms: 50,
   //totalSingles: 20,
   // singlesBooked: 5,
   // totalDoubles: 30,
   // doublesBooked: 10,
    
   // availableRooms: function() {
       // var arResult = this.rooms - (this.singlesBooked + this.doublesBooked);
       // return arResult;
//    }
//    }


function makeSandwichWith(filling) {
    Get one slice of bread;
    Add filling;
    Put a slice of bread on top;
}

makeSanwichWith(ham)